name: Build

on: [push, pull_request]

jobs:
  build:
    name: Build
    env:
      SCHEME: "EGPieChart"
      DEVICE: "iPhone 12"
    runs-on: macOS-latest
    steps:
    - name: Checkout project
      uses: actions/checkout@v1
    - name: Build application
      run: |
        set -o pipefail && xcodebuild clean -scheme SCHEME−destination"platform=iOSSimulator,name=DEVICE"
