name: Build

on: [push, pull_request]

jobs:
  build:
    name: Build
    runs-on: macOS-latest
    steps:
    - name: Checkout project
      uses: actions/checkout@v1
    - name: Build EGPieChart
      run: xcodebuild build
